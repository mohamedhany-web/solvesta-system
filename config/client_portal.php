<?php

return [

    /*
    |--------------------------------------------------------------------------
    | تنبيهات نشاط العميل للإدارة
    |--------------------------------------------------------------------------
    |
    | عناوين بريد تستقبل إشعاراً عند إنشاء تذكرة / بلاغ موقع / طلب اجتماع من البوابة.
    | يمكن فصل عدة عناوين بفاصلة. إذا تُركت فارغة يُرسل الإشعار داخل النظام فقط.
    |
    */
    'alert_emails' => array_values(array_filter(array_map(
        'trim',
        explode(',', (string) env('CLIENT_PORTAL_ALERT_EMAIL', ''))
    ))),

];
